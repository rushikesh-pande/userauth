# Monitoring Enhancement: Prometheus Scrape Configuration for userauth
# Add this job to your prometheus.yml scrape_configs section.
scrape_configs:
  - job_name: 'userauth'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 15s
    scrape_timeout: 10s
    static_configs:
      - targets:
          - 'localhost:{{ PORT }}'   # Replace with actual host:port
        labels:
          service: 'userauth'
          environment: 'production'
