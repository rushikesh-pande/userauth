# Database Optimisation — userauth

Auto-generated by **Database Optimisation Enhancement Pipeline** — 2026-03-11

## What's included

| Component | File | Purpose |
|-----------|------|---------|
| Redis Cache Config | `src/.../db/config/CacheConfig.java` | Per-cache TTL, JSON serialisation |
| Read-Replica Routing | `src/.../db/config/DataSourceRoutingConfig.java` | Routes readOnly → replica |
| Routing Aspect | `src/.../db/aspect/ReadOnlyTransactionAspect.java` | Auto-routes @Transactional(readOnly=true) |
| JPA Optimisation | `src/.../db/config/JpaQueryOptimisationConfig.java` | Batch DML, slow-query log |
| DB Metrics | `src/.../db/service/DatabaseMetricsService.java` | Cache hit/miss, query counters |
| DB Health | `src/.../db/health/DatabaseHealthIndicator.java` | Primary DB probe |
| Redis Health | `src/.../db/health/RedisHealthIndicator.java` | Redis cache probe |
| Flyway V1 | `src/main/resources/db/migration/V1__baseline_indexes.sql` | Performance indexes |
| Flyway V2 | `src/main/resources/db/migration/V2__cache_audit_table.sql` | Cache audit table |
| Docker Compose | `database/docker-compose-db.yml` | Redis + ES local stack |

## Quick Start

```bash
# Start Redis (and optionally Elasticsearch)
docker-compose -f database/docker-compose-db.yml up -d

# Verify Redis
redis-cli -h localhost ping        # PONG
# Redis Commander UI: http://localhost:8081

```

## Using the Cache Layer

```java
// Read — cache on first call, serve from Redis on subsequent calls
@Cacheable(value = "userauthCache", key = "#id")
public MyEntity findById(Long id) { ... }

// Write — update cache entry
@CachePut(value = "userauthCache", key = "#result.id")
public MyEntity save(MyEntity entity) { ... }

// Delete — evict from cache
@CacheEvict(value = "userauthCache", key = "#id")
public void delete(Long id) { ... }
```

## Using Read-Replica Routing

```java
// Reads go to replica automatically
@Transactional(readOnly = true)
public List<MyEntity> findAll(Pageable pageable) { ... }

// Writes go to primary automatically
@Transactional
public MyEntity save(MyEntity entity) { ... }
```

## Preventing N+1 Queries

```java
// In @Entity class — batch-fetch child collections
@OneToMany(fetch = FetchType.LAZY)
@BatchSize(size = 20)
private List<OrderItem> items;

// In @Repository — fetch-join instead of lazy loading
@Query("SELECT o FROM Order o JOIN FETCH o.items WHERE o.id = :id")
Optional<Order> findByIdWithItems(@Param("id") Long id);
```

## HikariCP Pool Monitoring

```
GET /actuator/metrics/hikaricp.connections.active
GET /actuator/metrics/hikaricp.connections.idle
GET /actuator/metrics/hikaricp.connections.pending
```
